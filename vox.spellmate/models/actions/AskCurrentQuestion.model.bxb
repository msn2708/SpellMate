action (AskCurrentQuestion) {
  type(Search)
  description (Initialize counter )
  collect {
    input (announcer) {
      type (Announcer)
      min (Required) max (One)
    }
  }
  output (Announcer)
}
