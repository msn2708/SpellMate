action (TerminateGame) {
  type(Calculation)
  description (Terminate the Game)
  collect {
    input (announcer) {
      type (Announcer)
      min (Required) max (One)
    }
  }
  output (Announcer)
}
